<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zipcar.zipcarcodingtest" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ItemsMainActivity"
            android:label="@string/title_activity_items_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EditItemsActivity"
            android:label="@string/title_activity_edit_items" >
        </activity>
        <activity
            android:name=".BuyActivity"
            android:label="@string/title_activity_buy" >
        </activity>
    </application>

</manifest>
